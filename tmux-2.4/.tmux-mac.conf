bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi Enter send -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi MouseDragEnd1Pane send -X copy-pipe-and-cancel "pbcopy"

#set-option -g status-right "#(get_load_average_tmux)#(get_volume_tmux)#(get_battery_tmux)#(get_ssid_tmux)#[fg=colour014] %H:%M | %m/%d %a#[default]"
set-option -g status-right "#(tmux-functions load_average)#(tmux-functions volume)#(tmux-functions battery)#(tmux-functions ssid)#[fg=colour014] %H:%M | %m/%d %a#[default]"

